

export const productReducer=(state={

},
action)=>{
        switch (action.type){
                case    :
                        return {} 
                case    :
                        return {}
                default:
                        return state;

        }

}